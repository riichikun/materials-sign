{% trans_default_domain 'materials-sign.admin' %}

<div class="modal-dialog modal-dialog-centered" style="max-width: 800px;">

    {{ form_start(form) }}

    <div class="modal-content p-3 border-bottom border-5 border-danger">

        <div class="modal-header">
            <h5 class="modal-title">
                {{ 'page.reissue'|trans }}
            </h5>
        </div>

        <div class="modal-body">
            <h4>
                {# Вы уверены, что желаете удалить #}
                {{ ' Вы действительно хотите осуществить перевыпуск?'|trans }}
            </h4>
            <br>
            <p>
                Все зарезервированные «честные знаки» будут возвращены в реализацию и затем снова будет запущен процесс
                резервирования под все товары в заказе и их количество.
            </p>
        </div>

        <div class="modal-footer ">

            <div class="mt-3 gap-3 d-flex">
                {# Отмена #}
                <button type="button" class="btn btn-light" data-bs-dismiss="modal">
                    {{ 'btn.cancel'|trans({}, 'core.btn') }}
                </button>

                {# Сохранить #}
                {{ form_widget(form.material_signs_reissue, { label: '
                    <span>'~ 'btn.save'|trans({}, 'core.btn') ~'</span>
                    <span class="spinner-border spinner-border-sm vertical-middle d-none"></span>
			    '}) }}
            </div>
        </div>

    </div>

    {{ form_end(form) }}
</div>